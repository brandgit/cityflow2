# Variables d'environnement pour CityFlow Analytics
# Configuration pour développement local et production AWS

# ============================================
# Configuration AWS
# ============================================
AWS_REGION=us-east-1

# DynamoDB Tables
DYNAMODB_METRICS_TABLE=cityflow-metrics
DYNAMODB_REPORTS_TABLE=cityflow-daily-reports

# S3 Buckets
S3_REPORTS_BUCKET=cityflow-reports
S3_REPORTS_PREFIX=reports

# ============================================
# Chemins de données (développement local)
# En production EC2, ces chemins pointent vers S3 ou EFS
# ============================================
# Décommenter et adapter pour la production :
# DATA_DIR=/mnt/efs/data
# BATCH_DATA_PATH=/mnt/efs/data/batch
# API_DATA_PATH=/mnt/efs/data/api
# COMPTAGES_CSV=/mnt/efs/data/batch/comptages-routiers-permanents-2.csv
# CHANTIERS_CSV=/mnt/efs/data/batch/chantiers-perturbants-la-circulation.csv
# REFERENTIEL_CSV=/mnt/efs/data/batch/referentiel-geographique-pour-les-donnees-trafic-issues-des-capteurs-permanents.csv

# ============================================
# Mode de fonctionnement
# ============================================
# Pour forcer l'utilisation de DynamoDB même en local (nécessite boto3 configuré)
USE_DYNAMODB=false

# Pour forcer l'utilisation de S3 même en local (nécessite boto3 configuré)
USE_S3=false

# ============================================
# Logs
# ============================================
LOG_LEVEL=INFO
